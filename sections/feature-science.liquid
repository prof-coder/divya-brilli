<div class="full_width">
  <div class="metricpage-width">
      <div class="section__header title-holder aa">
        {% if section.settings.heading != blank %}
        <h2><small><strong>{{section.settings.heading}}</strong></small></h2>
        {% endif %}
        {% if section.settings.heading != blank %}
        	<p>{{section.settings.subheading}}</p>
        {% endif %}
      </div>
    <div class="features">
      {% for block in section.blocks %}
      <div class="feature_part">
        {% if block.settings.image != blank %}
        	<img src="{{block.settings.image | img_url: '1024x'}}"/>
        {% endif %}
        {% if block.settings.text != blank %}
        	{{block.settings.text}}
        {% endif %}
      </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Feature science",
    "settings": [
		{
          "type": "text",
          "id": "heading",
          "label": "Heading"
  		},
		{
          "type": "text",
          "id": "subheading",
          "label": "Sub-Heading"
  		}
	],
  	"blocks": [
    {
      "type": "Image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
		},
        {
          "type": "richtext",
          "id": "text",
          "label": "Text"
        }
  	]
  }
],
	"presets": [{
        "name": "Feature science",
        "category": "Science"
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
